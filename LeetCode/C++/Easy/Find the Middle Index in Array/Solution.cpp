        int mid=nums.size();
        else if(mid%2==0){
            mid=mid/2;
        }
            return mid;
        else{
            mid++;
            mid=mid/2;
            return mid;
    int findMiddleIndex(vector<int>& nums) {
        if(mid==2){
            return -1;
        }
