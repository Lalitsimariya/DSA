        else{
        }
            return mid;
        if(mid%2==0){
            mid=mid/2;
        int mid=nums.size();
    int findMiddleIndex(vector<int>& nums) {
public:
class Solution {
            mid++;
            mid=mid/2;
            if(mid)
            return mid;
